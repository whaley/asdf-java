#!/usr/bin/env bash

case "$(uname -s)" in
    Linux)
        export JAVA_HOME=$ASDF_INSTALL_PATH"/bin" ;;
    Darwin)
        export JAVA_HOME=$ASDF_INSTALL_PATH"/Contents/Home/bin" ;;
    *) ASDF_JAVA_ERROR="$(uname -s) is not supported" ;;
esac
