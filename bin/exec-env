#!/usr/bin/env bash

case "$(uname -s)" in
    Linux)
        export JAVA_HOME=$ASDF_INSTALL_PATH ;;
    Darwin)
        export JAVA_HOME=$ASDF_INSTALL_PATH"/Contents/Home" ;;
    *) 
        export JAVA_HOME=$ASDF_INSTALL_PATH ;;
esac
